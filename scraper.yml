name: DealRadar Scraper

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run scraper
        env:
          SUPABASE_URL: https://fdwdzahyeueaiigvazah.supabase.co
          SUPABASE_KEY: sb_publishable_d4lHwSTc23D4UtIMtXe0bA_EJhI__Ol
        run: node scraper.js
